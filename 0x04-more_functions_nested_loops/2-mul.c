#include "main.h"
#include <stdio.h>

/**
 * mul - return multiplicarion of a * b
 * @a: first number to multiply
 * @b: second number to multiply
 * Return: multiplication of a * b
 */

int mul(int a, int b)
{
	return (a * b);
}
